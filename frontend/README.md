# Frontend (React + Three.js)

Dev:

```bash
cd frontend
npm install
npm run dev
```

Tests:

```bash
cd frontend
npm install
npm test
```

Build:

```bash
npm run build
```

The Three.js scene is in `src/App.jsx`. This project is configured to be built as a static site for GitHub Pages/Netlify/Vercel.
